export class Ingredient {
constructor(public name: string, public amount: number) {}  
}

//public name: string;
//public amount: number;
//this.name = name;
//Instead of writing all these we can add public infront of the property name